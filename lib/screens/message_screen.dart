import 'package:flutter/material.dart';

class MessageScreen extends StatelessWidget {
  static const String routeName = 'message';
  
  const MessageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // final args = ModalRoute.of(context)?.settings.arguments ?? 'No-data';
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mensajería seXquare'),
      ),
      body: SingleChildScrollView(
        child: Padding(
          padding: const EdgeInsets.all(10.0),
          child: Column(
            children: const [
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomRight,
                child: Text('Aqui va tabla mensajes de texto')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomRight,
                child: Text('Aqui va tabla mensajes de texto')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomRight,
                child: Text('Aqui va tabla mensajes de texto')),
              Align(
                alignment: Alignment.bottomRight,
                child: Text('Aqui va tabla mensajes de texto')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomRight,
                child: Text('Aqui va tabla mensajes de texto')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomRight,
                child: Text('Aqui va tabla mensajes de texto')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomRight,
                child: Text('Aqui va tabla mensajes de texto')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomRight,
                child: Text('Aqui va tabla mensajes de texto')),
              Align(
                alignment: Alignment.bottomRight,
                child: Text('Aqui va tabla mensajes de texto')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
              Align(
                alignment: Alignment.bottomRight,
                child: Text('Aqui va tabla mensajes de texto')),
              Align(
                alignment: Alignment.bottomLeft,
                child: Text('Aqui va tabla mensajes de texto emitidosy recibidos por los usuarios exclusivos de seXquare')),
            ],
          ),
        )
      ),
    );
  }
}
